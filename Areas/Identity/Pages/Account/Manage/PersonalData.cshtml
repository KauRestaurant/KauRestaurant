@page
@model PersonalDataModel
@{
    ViewData["Title"] = "البيانات الشخصية";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<div class="text-center mb-4">
    <h4 class="mb-3">@ViewData["Title"]</h4>
</div>

<div class="alert alert-warning">
    <div class="d-flex">
        <div class="me-3">
            <i class="bi bi-exclamation-triangle-fill fs-3"></i>
        </div>
        <div>
            <h5 class="alert-heading">تنبيه مهم</h5>
            <p class="mb-0">
                حذف هذه البيانات سيؤدي إلى إزالة حسابك بشكل نهائي، ولا يمكن استعادة هذه البيانات بعد الحذف.
            </p>
        </div>
    </div>
</div>

<div class="text-center">
    <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">
        <i class="bi bi-trash me-2"></i>
        حذف الحساب نهائياً
    </a>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
