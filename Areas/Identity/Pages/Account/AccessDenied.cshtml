@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "تم رفض الوصول";
    Layout = "_AdminLayout";
}

<!-- Main Content Section -->
<div class="container-fluid py-4 py-lg-5 bg-white">
    <div class="container max-w-600">
        <!-- Access Denied Card -->
        <div class="card border-0 shadow">
            <div class="card-header bg-white py-3">
                <h3 class="fw-bold mb-0 text-danger">تم رفض الوصول</h3>
            </div>
            <div class="card-body p-4 text-center">
                <div class="mb-4">
                    <i class="bi bi-exclamation-circle text-danger" style="font-size: 4rem;"></i>
                </div>
                <p class="mb-4">ليس لديك صلاحية للوصول إلى هذا المورد</p>
                <div class="d-grid gap-2 col-md-6 mx-auto">
                    <a href="javascript:history.back()" class="btn btn-success">
                        <i class="bi bi-arrow-left me-2"></i>العودة
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
